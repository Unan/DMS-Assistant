import {InsuranceTypePipe} from './insurance-type.pipe';

describe('InsuranceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InsuranceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
